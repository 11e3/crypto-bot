services:
  vbo-bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./logs:/app/logs
      - ./bot:/app/bot:ro
      - ./bot.py:/app/bot.py:ro
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
